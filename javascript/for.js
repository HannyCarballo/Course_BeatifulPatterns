// ciclo for (para)
// inicio
// condición para terminar
// actualización del valor del contador
for (var contador=1; contador <= 5; contador++) {
    // los pasos que la consola necesita hacer si la
    // condición esté satisfecha
    console.log(contador)
}